import { useQuery } from '@apollo/client'
import React from 'react'
import { GET_BOOKS } from '../../utils/queries'

function Home() {
  return (
    <div>
      {/* Books container */}
    </div>
  )
}

export default Home